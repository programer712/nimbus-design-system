export interface ShapeOptions {
  borderRadius: number;
  borderRadiusXs: number;
  borderRadiusSm: number;
  borderRadiusMd: number;
}

const BorderRadius: ShapeOptions = {
  borderRadiusXs: 4,
  borderRadius: 8,
  borderRadiusSm: 12,
  borderRadiusMd: 16,
};

export default BorderRadius;
